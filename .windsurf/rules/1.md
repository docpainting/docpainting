---
trigger: always_on
---

Exit Code 130 when using the terminal is a false positive. The command runs in the users terminal. Request the outcome if this is to happen prior to running another command. Once youve interpreted the new results from the user you can run another command.